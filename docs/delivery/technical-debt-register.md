# Technical Debt Register

**Project**: PFM Bot
**Last updated**: 2025-12

## Legend

- **Impact**: What breaks or degrades if this is not addressed (Security / Correctness / UX / Ops)
- **Effort**: S = hours, M = 1–3 days, L = 1+ week
- **Priority**: P0 = fix now, P1 = fix soon (next sprint), P2 = fix eventually, P3 = known tradeoff / low urgency
- **Status**: Open / In Progress / Done

---

| ID | Item | Area | Impact | Effort | Priority | Status | Notes |
|----|------|------|--------|--------|----------|--------|-------|
| TD-001 | No rate limiting on API | API / Security | Any user or bot can flood `POST /tg/expenses` or spam `/tg/dashboard`, causing DB overload | S | P1 | Open | Express `express-rate-limit` middleware; apply per `userId` extracted from `X-TG-Init-Data` |
| TD-002 | Avalanche plan estimate ignores APR changes over time | Formula | `buildAvalanchePlan()` simulates payoff with a fixed APR for all months. Variable-rate credit cards can change. Estimate may be off by 10–20% for variable-rate debts. | M | P2 | Open | `apps/api/src/avalanche.ts` line 106; would need APR schedule input |
| TD-003 | Period rollover at 00:05 UTC, not user's local midnight | Cron / Correctness | Users in UTC+5 to UTC+12 (Yekaterinburg, Vladivostok) have their period roll over mid-morning, not at midnight. Expenses recorded before rollover may land in the wrong period. | L | P2 | Open | Rollover cron in `apps/api/src/cron.ts`; fix requires per-user rollover scheduling |
| TD-004 | DailySnapshot saved at 23:55 UTC, not per-user timezone | Cron / Correctness | For Moscow users (UTC+3) the snapshot is at 02:55 AM — fine. For Vladivostok (UTC+10) it's 09:55 AM — snapshot captures mid-day state, not end-of-day. `isOverspent` flag is misleading for eastern timezones. | M | P2 | Open | `apps/api/src/cron.ts` line 156; fix: run per-user snapshot at 23:55 local time |
| TD-005 | No idempotency key for expense creation | API / UX | Slow network + user retry creates duplicate expense records. User must notice and manually delete. | S | P1 | Open | Add `Idempotency-Key` header handling in `POST /tg/expenses`; store key with 60s TTL in DB or Redis |
| TD-006 | GOD_MODE grants full access with no audit log | Security / Compliance | Admin-level access via `GOD_MODE_TELEGRAM_IDS` env var. No `AdminAuditLog` table. No record of which god-mode user accessed or modified what. | M | P2 | Open | `apps/api/src/index.ts` `ensureUser`; add audit middleware for god-mode requests |
| TD-007 | `/delete` user data command not implemented | Bot / Compliance | Users cannot delete their account and all associated data via the bot. Required for GDPR-style compliance (right to erasure). | M | P1 | Open | Implement `/delete` in `apps/bot/src/index.ts`; should call a new `DELETE /tg/me` API endpoint that cascades delete via Prisma (all models have `onDelete: Cascade`) |
| TD-008 | No import/export of expenses | UX / Data portability | Users cannot export their expense history (CSV/JSON) or import from bank statements. The `ExpenseSource.IMPORT` enum value exists in the schema but is unused. | L | P3 | Open | Out of scope for MVP; `Expense.source` field is reserved for future use |
| TD-009 | Notification dedup is in-memory, lost on restart | Cron / UX | `notifLog` Map in `apps/api/src/cron.ts` is lost on process restart. If API restarts at exactly 09:00 Moscow time, morning notification fires again. | S | P1 | Open | Move dedup to `NotificationLog` DB table with `userId + type + date` unique constraint |
| TD-010 | EF contribution not resumed when target months increases | Formula / Correctness | If user changes `targetMonths` from 3 to 6 after EF is fully funded, the engine correctly recalculates `efDeficit` and resumes contributions. However, there is no UI prompt or notification informing the user that their daily limit has decreased due to the new EF target. | S | P2 | Open | Add a `recalculate` trigger on EF settings save; notify user of limit change |
| TD-011 | Period does not store `triggerPayday` — computed at runtime | Data model / Correctness | The engine recomputes `triggerPayday` from `endDate + allPaydays` on every dashboard request. If paydays change mid-period, the trigger for the current period is recalculated incorrectly. E.g., user changes payday from 15th to 20th mid-period: the trigger changes retroactively. | M | P1 | Open | Add `triggerPayday Int?` column to `Period` table; persist at period creation time |
| TD-012 | No skeleton loaders on initial web page load | UX | `apps/web` shows layout shift while API data loads. Dashboard numbers flash from "0" to real values. Perceived performance is poor on first open. | S | P2 | Open | Add Tailwind skeleton pulse classes to dashboard number slots |
| TD-013 | Bot `/spend` command uses `parseFloat` but description says integers | Bot / UX | `apps/bot/src/index.ts` line 154: `const amount = parseFloat(parts[0])`. This actually parses decimals correctly (e.g., `/spend 1500.50`). However, the help text says "сумма в рублях" without clarifying decimals are supported. The amount is converted to kopecks with `Math.round(amount * 100)`, so `/spend 1500.50` → `150050` kopecks correctly. The actual bug is the misleading documentation, not the parsing. | S | P3 | Open | Update `/help` text to clarify decimal amounts are accepted |
| TD-014 | No alert when subscription is about to expire | Cron / UX | When a user's PRO subscription (`Subscription.currentPeriodEnd`) is within 3 days of expiry, no notification is sent. Users discover their subscription has lapsed when PRO features stop working. | S | P2 | Open | Add a Cron 5 in `apps/api/src/cron.ts`: daily check for subscriptions expiring in ≤3 days, send Telegram notification |
| TD-015 | Avalanche pool not zero-checked before `min()` with focus debt balance | Formula / Correctness | In `engine.ts` lines 197–204: `avalanchePool = Math.round(investPool * 0.50)` is called before checking `investPool >= 0`. If `freePool - efContribution` is negative (tight budget), `investPool` is negative and `avalanchePool` becomes negative before the `min(avalanchePool, focusDebt.balance)` clamp. `Math.min(negativeNumber, positiveBalance)` returns the negative number. The outer `residual` calculation then adds back a negative pool, inflating `s2sPeriod`. | S | P0 | Open | Add `avalanchePool = Math.max(0, avalanchePool)` after line 204 in `engine.ts` |
| TD-016 | Bot calls API with `X-TG-DEV` header in all environments | Security | `apps/bot/src/index.ts` lines 100 and 167: bot uses `X-TG-DEV` header to call the API, bypassing HMAC initData verification. In production, `NODE_ENV=production` disables the dev bypass in `tgAuth` middleware — but the bot still sends `X-TG-DEV` which is silently ignored (401 returned). The bot's `/today` and `/spend` commands fail silently in production since they lack a valid `X-TG-Init-Data`. | M | P1 | Open | Bot should call API via `/internal/*` routes using `X-Internal-Key`, or the bot should construct a valid initData for the user context |
| TD-017 | `GET /tg/expenses/today` uses UTC midnight, not user's local midnight | API / Correctness | Query: `spentAt >= new Date().setHours(0, 0, 0, 0)` is UTC midnight. Moscow users' expenses between 00:00 and 02:59 local time appear as "yesterday." Affects both the dashboard display and the S2S today calculation. | M | P2 | Open | Pass user timezone from DB and compute local midnight in the query |
| TD-018 | pnpm lockfile mismatch on deploy when package.json changes | Ops / Deploy | If `package.json` is edited locally and `pnpm-lock.yaml` is not regenerated before committing, `pnpm install --frozen-lockfile` (used in Dockerfiles) fails on the server. Requires manual `pnpm install` + rebuild. | S | P1 | Open | Add a pre-commit hook (`husky`) that verifies lockfile is up-to-date; or switch Dockerfiles to `pnpm install --no-frozen-lockfile` in non-CI builds |
